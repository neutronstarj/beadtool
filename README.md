turn image to fuse bead teamplate
color mapping to mard

to start

docker compose up --build


todos: 
- prettyify the UI 
- x,y axis reference
- crop image 
- color bar at the bottom to reference
- export as png 
- deploy 
